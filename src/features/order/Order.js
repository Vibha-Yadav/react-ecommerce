import React, { useState } from 'react';
import { useSelector, useDispatch } from 'react-redux';
import {
  increment,
  createOrderAsync,
  
} from './orderSliceSlice';
// import styles from './Counter.module.css';

export default function Order() {
  
  const dispatch = useDispatch();

  return (
    <div>
      <div >
        
      </div>
    </div>
  );
}
